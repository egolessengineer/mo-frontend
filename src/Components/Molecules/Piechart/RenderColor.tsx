export const renderColor = (value: string, entry: any) => {
  return (
    <span
      style={{
        color: "#74777A",
        fontWeight: 500,
        padding: "10px ",
        fontSize: "14px",
        transform: "rotate(90deg)",
      }}
    >
      {value}
    </span>
  );
};
